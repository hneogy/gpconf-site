# Decisions (gpconf-site)

Append-only. Corrections are new entries. Numbered S-001… to keep them apart from the corpus's D-numbers.
From S-028 on, every entry carries its date after its number; entries before S-028 are undated (git history dates them).
A claim not verified in the session carries [untested] or [inferred] inline and a line under "Open questions"
in HANDOFF.md.

**S-001 — Static site, Python build, no framework.** Seven pages of mostly prose with two reading levels each, plus small client-side tools and charts, do not need a JavaScript framework. The build is one Python script (`build.py`, Jinja2 templates, ~150 lines) producing plain HTML into `dist/`; the scheduled jobs are Python too, so the repository has one language. Jinja2 is the only dependency and is pinned in `requirements.txt`. Considered and rejected: Eleventy or Astro (a Node toolchain for template inheritance alone), hand-written HTML (seven pages × shared header, footer and toggle would drift), committing `dist/` (generated noise; Cloudflare Pages builds it).

**S-002 — Hosting: Cloudflare Pages Git integration, Python build.** Cloudflare's current (v3) Pages build image ships Python 3.13 and pip and honours `.python-version`; the build command is `pip install -r requirements.txt && python build.py`, output directory `dist`. Every commit, including the jobs' data commits, triggers a rebuild, which is how the pages get fresh values without client-side fetching. To be confirmed when the project is connected; the fallback is a GitHub Actions deploy with `wrangler pages deploy`, which needs an API token the owner would create.

**S-003 — Pages are rendered from the jobs' JSON at build time.** The home, tracker and library pages carry their numbers in the HTML, so they work without JavaScript and search engines see them. JavaScript adds only the history charts (fetching `/data/tracker/history.json` from the same origin) and the two tools. Nothing on any page contacts anything but this origin; the CSP (`default-src 'none'; script-src 'self'; style-src 'self'; connect-src 'self'; form-action 'none'`) enforces it. No external fonts, images or scripts, so the Zenodo DOI badge is a text link.

**S-004 — Reading level is a client-side toggle over duplicated content, default Simple.** Each page contains both versions in `.lvl-simple` and `.lvl-technical` blocks; a pre-paint script applies the stored choice (`localStorage`, key `gpconf-level`) or `?level=` and defaults to Simple; with scripts off both versions show with labels. Rejected: two URL trees (double the pages to link and index; the toggle would be a navigation) and server-side variants (the choice must persist across pages instantly).

**S-005 — Fetch budget and rotation.** Five daily endpoints (the last-30-days CSV and TLE, the analyst CSV and TLE, the Starlink SupGP CSV) plus stable-source drift checks. The corpus has 22 stable CelesTrak sources; re-fetching all of them one day a week would be 27 requests that day, over the owner's ~10/day. Instead each source is due 6.5 days after its last check and the job takes at most 4 due sources per run, oldest first, so every source is checked about weekly and no run exceeds 9 requests. The fetcher refuses a repeated URL and enforces a hard cap of 10 before sending anything; no redirects, no retries, 2 s between requests, a User-Agent naming the project and site; tested in `tests/test_tracker.py`. One expected 404 a day (the last-30-days TLE) is far under CelesTrak's 50-errors-per-2-hours threshold.

**S-006 — Derived measurements only, by construction.** The tracker computes record counts, the highest id, six- and nine-digit counts, the HTTP status of the TLE request, count relations, and per-source SHA-256 comparisons; the outputs never contain a field other than `NORAD_CAT_ID`-derived integers, statuses, byte counts, hashes and timestamps. Raw bodies stay in memory in CI; a local `--cache-dir` (git-ignored) exists for inspection. A test asserts that no element value from the test input appears in any output. The library job stores verdicts, versions and exception text only.

**S-007 — Worked examples.** The only element sets on the site are (a) the CCSDS 502.0-B-3 annex G OMM example (GOES 9) as printed in the standard and as rendered into a TLE by the corpus renderer (checksums verified), used for the side-by-side, the anatomy tool, the highlighted provider-convention columns and the library reproducers; and (b) the corpus's published derived Alpha-5 line for catalog 100000 (SARAMAGO, first record), whose provenance sidecar is public. No CelesTrak element set is displayed otherwise, no SupGP value anywhere, nothing from Space-Track. The provider rendering differences are shown as highlighted columns on the annex G line, conventions only.

**S-008 — Seed history.** The tracker history starts with one entry dated 2026-09-21 built from the public corpus's published Phase 2 counts (last-30-days: 256 records, all six-digit, highest 100789, TLE 404; analyst: 565 CSV vs 219 TLE, 346 six-digit; Starlink SupGP: 27 nine-digit ids). It is marked `kind: seed`, drawn in amber on the charts, and states that no request was made by this site for it. The total record count of the Starlink SupGP file at the snapshot was not published by the corpus and is left null.

**S-009 — Weekly drift check semantics.** Match/drift/error per source, from the SHA-256 in the corpus manifest (v0.1.0, commit fdf4043). Two stable sources are 16-byte 404 bodies (`No GP data found` for a six-digit object's gp-first TLE); a match there confirms the 404 semantics, and a 200 there would be reported as drift, which is the right alarm.

**S-010 — Library job runs against the latest PyPI releases, in CI's own environment.** `pip install --upgrade sgp4 skyfield` then the reproducers; recorded: versions, whether python-sgp4's C++ extension is active (the classification result on the TLE path depends on it), Python version. Skyfield's timescale uses its bundled data so the job makes no network request of its own. Inputs are annex G values and the vendored vectors (SHA-256 in `data/vectors/PROVENANCE.md`).

**S-011 — Licence and attribution.** MIT, copyright NEOGY LLC, as the corpus. Maintainer named as Honorius Neogy. The site states on every page that it was built with AI assistance under human direction and links to the explanation.

**S-012 — Not done until the owner says so.** No GitHub repository, no Cloudflare configuration, no push, no DNS. The site repository link on the tracker and library pages is given as a path (`jobs/tracker.py`) rather than a URL until the repository exists.

**S-013 — Local runs on 2026-09-21 (owner's instruction: run both jobs once, then stop).** Tracker: 9 requests, 0 errors, 1.96 MB, 05:51–05:52 UTC; last-30-days CSV 231 records, all six-digit, highest 100789, TLE request 404; analyst CSV 594 records (375 six-digit, 219 below 100000) and TLE 219 records, relation holds; Starlink SupGP CSV 11,141 records with 27 nine-digit ids present; drift rotation took the four never-checked `gp-first.php?CATNR=100000` sources, all match (including the 16-byte 404 body). Raw bodies went to a git-ignored local cache, were used once for a leak check (2,318 distinctive values from the bodies searched across every JSON, HTML and JS output: none found) and then deleted. Library job in a throwaway environment with the latest PyPI releases, python-sgp4 2.27 (accelerated build) and Skyfield 1.55: 3 pass, 6 fail, matching the corpus's findings; the classification-through-`twoline2rv` row passes on the accelerated build, as expected. The history therefore starts with two entries for 2026-09-21, the corpus seed and this run; the scheduled job replaces same-day scheduled entries only, never the seed. Nothing was pushed or deployed; no repository or Cloudflare configuration was created.

**S-014 — Activity section: a Pages Function with a two-tier edge cache, static attributions, hand-maintained timeline.** `/api/activity` (`functions/api/activity.js`) makes seven GitHub REST calls per refresh (issues #169 and #171, PRs #170 and #172, the corpus's latest release, the corpus and site repositories) and keeps only state, dates, counts and links: no titles, bodies or comment text enter the output, which a node test asserts with a fake GitHub. Cache: Cloudflare's Cache API with a one-hour "fresh" entry and a 30-day "last good" entry; a GitHub failure serves the last good copy with its original timestamp, `stale: true` and the reason; with no copy at all the function answers 503 and the page keeps its static links. The Cache API is per edge location, so "one refresh per hour" holds per location, not globally; documented in the README, and a KV binding could make it global later if wanted. Authentication: `env.GITHUB_TOKEN` (Pages secret, read-only public-repositories fine-grained token); the function works unauthenticated too. The four attributions are fixed strings in the template, tested for exact single occurrence; the topic lines are the site's own neutral wording, not upstream titles. The timeline is `data/timeline.json`, hand-maintained, seeded with six recorded milestones, each linking to a public record, with public timestamps where they exist (release, issue, PR). Both reading levels: Simple sentences per item are chosen from the live state (open issue, open/draft/merged/closed pull request).

**S-015 — Pre-publication check and publication of the repository (owner authorisation).** Scan over the 51 git-tracked files: TLE-shaped lines: only the CCSDS annex G rendering (U and, in the library job, C) and the published derived SARAMAGO line; epochs and mean motions: only annex G and SARAMAGO values; no OMM CSV rows; no Space-Track data (the one hit is the test that forbids it); no reference to the private corpus repository; no secret (the one hit is the test's literal "not-a-real-token-for-test"); no personal e-mail address in any file (the workflows commit as GitHub's own `github-actions[bot]` noreply identity, the standard form; commit metadata carries the same author identity already public on the corpus commits); `.dev.vars` and other secret-shaped files are neither present nor tracked; `dist/`, virtual environments and caches are ignored. Two hygiene edits first: the workflows' made-up bot addresses replaced by GitHub's noreply identity, and a token-prefixed fake in the node test replaced by a plain string. Then: public repository https://github.com/hneogy/gpconf-site created with a one-line description and ten topics, `main` pushed (aafee98), Actions enabled with default workflow permissions set to write so the scheduled tracker and library jobs can commit their JSON; the three workflows are active. Cloudflare was not touched; the tracker was not run again today (its schedule starts tomorrow 06:17 UTC).

**S-016 — Text corrections (owner instruction), pushed to main.** (1) Attribution made exact everywhere: the home page's Findings card no longer says "two bugs we reported"; it now states that one bug (#171) was found and reported by this project with a fix submitted (PR #172), and that a second bug (#169), reported by someone else, has a pending fix by another contributor (#170) that the corpus tested. The same sweep (every template, both reading levels, the timeline data and the library job's upstream links) corrected the findings page's item 3, the about page's upstream list and the library page's row explanation to say that the fix for #171 is PR #172, submitted at the maintainer's invitation and awaiting review; the library job now links PR #172 beside #171 for that behaviour, and its JSON was regenerated offline. No other claim of finding, reporting or fixing was present. (2) "Sixteen cases built from real CelesTrak responses" now ends "and published specification vectors". (3) "Older software quietly loses every satellite launched since" is now "Much older software can quietly lose every satellite catalogued since". (4) The home page's corpus list gained PR #172 after issue #171, in neutral wording. Tests (23) and the build pass. Observed before pushing: the custom domain gpconf.neogy.dev currently answers with a 301 to https://neogy.dev, while the Pages project is reachable at gpconf-site.pages.dev; deployment is therefore verified on the pages.dev host, and attaching the custom domain remains the owner's Cloudflare step.

**S-017 — Deployment of S-016 confirmed.** Commit fa4e549 pushed 2026-09-21T20:49:41Z; GitHub Actions "tests and build" succeeded; the "Cloudflare Pages" check-run on the commit succeeded; the new wording was live on https://gpconf-site.pages.dev about two minutes after the push, and all four edits were verified there (home hero, home Findings card, home corpus list with PR #172, findings item 3, about upstream list, library row). Also observed: `/api/activity` on the pages.dev host answers (cache MISS, site repository now seen) with `authenticated: false`, so the `GITHUB_TOKEN` secret is not yet in place; and https://gpconf.neogy.dev still answers 301 to https://neogy.dev, so the custom domain is not yet attached to the Pages project. Both are the owner's Cloudflare steps; nothing on Cloudflare was touched.

**S-018 — Site updated for the corpus's seventeenth case (writer-side `tle-writer-alpha5`) and `gpconf check-tle`; edited only, not committed, pushed or deployed (owner's steps).** Counts changed in both reading levels: "sixteen" to "seventeen" in the home page's technical lede and Findings card, the findings page's lede, heading and summary line, and the story's "If you build it" bullet. The findings table and `site/content/failures.json` were regenerated from the corpus's `docs/FAILURES.md` at commit caca6c5 (generated 2026-09-22T19:24:35Z), which is where every figure comes from: naive fails 14 of 17 cases (it passes epoch-year-19xx, baseline-iss-five-formats and satcat-70000-cutoff), python-sgp4 fails 7 of 17 with 20 failing items, the last of them the writer case's synthetic -1 input (it writes the Alpha-5 field correctly and refuses 340000 and nine-digit ids), and the reference passes all 17 exactly; the sixteen v0.1.0 rows were verified unchanged. Correction found on the way: the previous summary line said "Naive fails 14 of 16", while the v0.1.0 catalogue's table shows 13 of 16 with the same three passes, so the old figure was one too high; the new figure is read from the table, not carried over. Left as historical statements: the timeline's "16 cases" for the v0.1.0 release, and the two mentions of the PR #170 test, now worded "across the sixteen cases the corpus had at the time". `corpus.version` stays 0.1.0 in `build.py` until the owner tags; the findings lede says "v0.1.0 plus the writer-side case added on main since". The writer case is described where the table is introduced, in both levels (a program that writes the old format must put the right lettered plate on it and refuse numbers the plate cannot hold). One line added where a builder looks for how to test: the technical "What to change" close points a tool that writes TLEs at `python -m gpconf check-tle`; the glossary's Parser entry notes the writing side. No timeline entry, because the corpus change has no public record until it is pushed. Nothing restructured; no new page.

**S-019 — Site set to corpus v0.2.0 (owner instruction); edited only, not committed or deployed. Discrepancy found and left for the owner: at the time of the edit the public repository showed no v0.2.0 tag or release (main at 78ba712, sixteen cases, only tag v0.1.0), and Zenodo listed v0.1.0 only.** Changes: `corpus.version` 0.2.0 in `build.py`; the findings lede simplified to "the seventeen cases of corpus v0.2.0"; `site/content/failures.json` provenance and link point at tag v0.2.0 (generation stamp 2026-09-22T23:31:15Z, the catalogue prepared for that release); a v0.2.0 timeline entry in the shape of the v0.1.0 one, naming the writer-side case and `gpconf check-tle`, without a publish time or commit hash because none was public; the v0.1.0 entry untouched. Kept truthful under the new version: `build.py` gains `version_doi_version` (0.1.0) and the home and about pages say the version DOI is for v0.1.0 and that the v0.2.0 DOI is added once Zenodo has archived the release; the home page's corpus line says "independently audited at v0.1.0" instead of "audited before publication", since the audit covered v0.1.0. Before deploying, the owner confirms the tag is public, fills in the timeline entry's time and commit, and, when the v0.2.0 DOI exists, sets `version_doi`, `version_doi_url` and `version_doi_version` together.

**S-020 — Site completed for corpus v0.2.0 (owner instruction); edited only, not committed or deployed. The v0.2.0 timeline entry carries the GitHub release's publish time 2026-09-23T00:16:27Z and "(tag on commit b54c3b8)", in the shape of the v0.1.0 entry; its date is the UTC date of that time, 2026-09-23, as the timeline's provenance note requires (the corpus's own release date, 2026-09-22, is the date of the tagged content and stays in the corpus). `build.py` sets `version_doi` 10.5281/zenodo.22906966, `version_doi_url` https://zenodo.org/records/22906966 (the record page, as instructed; the concept DOI keeps its doi.org link) and `version_doi_version` 0.2.0, so the conditional clauses added in S-019 no longer render and the home and about pages state the version DOI for v0.2.0. The v0.1.0 timeline entries and their DOI stay as history, and a "Zenodo DOI minted" event for v0.2.0 (10.5281/zenodo.22906966, dated 2026-09-23) follows the release entry in the shape of the v0.1.0 one. Tests and the build pass; no stale count or DOI reference remains outside historical entries.

**S-021 — Build made tolerant of a failed tracker day, after the 2026-09-22 scheduled run recorded nine HTTP 500 responses from CelesTrak (identical 1,074-byte body; the job made its nine requests, no retries, and stopped, as the policy requires; a 500 is a server error, not a 403 policy block) and its data commit 5c1dc41 left the tracker page unbuildable.** Why it surfaced late: the job pushes with the Actions token, and such pushes do not trigger the `tests and build` workflow, so the first run to see the data was the owner's push 417c133, which CI marked failed (run 35802296212) and Cloudflare could not build; the previous deployment stayed live. Fix: `fmt_int` and `yesno` treat Jinja's `Undefined` as a missing value (dash, "unknown") instead of raising; the history table marks a day whose status is not `ok` with its status, so a failed day is visible rather than blank; a test pins both. The tiles keep showing the last successful measurements, as before. Recorded for the owner: consider letting the job's push trigger CI (a personal access token or a `workflow_dispatch` after the commit), so that data the job writes is built before it reaches Cloudflare.

**S-022 — Drift classification fixed and four false "drift" results repaired.** On 2026-09-22 at 06:44 UTC every request of the scheduled run, including the four weekly drift checks (`gp-first-100000-XML`, `gp-first-25544-2LE`, `gp-first-25544-CSV`, `gp-first-25544-JSON`), received HTTP 500 with the same 1,074-byte error page, SHA-256 `e6d27d46a908d95c2b7a93ca5a1329dfb80eeea54a9c35ec0e150cf2f2623872`. The fetcher records an HTTP error's status and body without setting `error`, and `run_drift` compared the hash of anything without `error`, so the error page was compared with the recorded hashes and all four sources were marked "drift"; the tracker page then showed "4 drift" in red from that morning until this fix was deployed, a false statement about the corpus's gp-first stability assumption. Rule now (`classify_drift`): a 200, or the 404 recorded for the two 16-byte "No GP data found" sources, is an answer and is hashed; a 200 where 404 was recorded or the reverse is "drift", the alarm S-009 asks for; any other status (5xx, 403, 429), or a transport failure, is "error". An error does not touch `last_checked`, so the source stays due, and does not record the page's hash as the source's; it sets `last_error_at`. The tile shows the error count and turns amber on errors; the per-source table shows the error time. A regression test covers a 500 page, a matching 404, a 404 that became data, and a source whose earlier success stands. Repair of the recorded data, with the values shown in the commit: the four sources' `last_status` "drift" → "error", `last_checked` (2026-09-22T06:44:55Z to 06:45:01Z) → null with that time moved to `last_error_at`, the error-page `last_sha256` removed, their history entries "drift" → "error"; the day's `drift.results` in `history.json` "drift" → "error"; `latest.json`'s drift summary recomputed (4 match, 0 drift, 4 error, 14 never checked) with its fetch log kept. All 22 recorded baseline hashes, byte counts and statuses are byte-identical to the committed file, so a real drift stays detectable.

**S-023 — A failed day no longer reads as an answer in the TLE-404 chart and the history table.** On a day whose TLE fetch failed, `last30_tle` carries `is_404: false` (a 500 is not a 404), so the boolean chart coloured 2026-09-22 green, "TLE data returned", and the noscript table said "no"; both were wrong statements about a day with no answer. `charts.js` now gives both boolean charts an `okPath` and `boolChart` renders a day whose measurement is not `ok` as the neutral "no data" cell; the history table's TLE-404 cell is a dash unless the measurement is `ok`. Tests: the failed-day row of the built page is six dashes, and the chart script's contract is pinned textually because CI has no JavaScript runner (a separate item, S-021 note).

**S-024 — Both scheduled jobs validate their data before committing it; a run's data is preserved as an artifact; the push rebases first; `ci.yml` can be run by hand.** Cause: pushes made with the Actions token do not trigger workflows, so the tracker's data of 2026-09-22 reached Cloudflare with no test run (S-021). Shape now, in `tracker.yml` and `library.yml` alike: the job's step writes its printed summary to the run's step summary; `actions/upload-artifact@v4` then stores the three (two) JSON files and the log for 30 days, unconditionally once the run step succeeded; a validation step installs the site's one dependency, runs the test suite and `build.py` against the working tree, which is today's data; only then the commit step runs, with `git pull --rebase origin main` before the push so a manual push during the run cannot lose the day. On a validation failure the job is red, GitHub notifies the workflow's author, nothing is committed, the artifact holds the data, and the next day's run starts from `main` (the day is a gap, restorable by committing the artifact's files after the fix; not by re-running the job, which would repeat the requests). Artifacts of a public repository are downloadable by any GitHub account; these files hold counts, statuses, hashes and timestamps that the site already publishes. No nightly schedule for `ci.yml`: the two bot pushes were the only unvalidated path into `main`, and every other push triggers CI already; a `workflow_dispatch` trigger is added instead. Not changed: the JavaScript tests are still not run in CI (S-021 note).

**S-025 — Hand-written observations about third-party software live in a labelled, hand-maintained "Elsewhere in the ecosystem" section at the foot of the library page, backed by `site/content/ecosystem.json`, never in a job-generated table; a dated timeline event records when the statement was made.** First entry: SatNOGS. Fredy, a SatNOGS maintainer, wrote on the Libre Space forum on 2026-09-22 (post-level permalink, public timestamp 06:28:37 UTC) that no SatNOGS component supports NORAD ids above 99999 and that support for Alpha-5 and OMM JSON is in progress, with the level differing by sub-project. Placement: the library table's rows are results the weekly job computed, so a SatNOGS row would claim a test that was never run; the section sits outside the `{% if L %}` block after Run history, with the same "Hand-maintained" note the timeline carries and the sentence "Statements, not results: nothing in this list has been run against the corpus." Rejected: the findings page, whose closing rule is that every statement links to a case in the test kit; the tracker, job data only; the activity page's upstream cards, whose state comes from the GitHub API and would never update for a GitLab-and-forum project; the about page's Upstream list, which is for reports filed from the corpus. Wording rules, from the owner's review of the draft: the maintainer is named and the link is the post-level permalink; no date estimate is put on someone else's work ("said to be in progress", not "expected within weeks"); the site's own offer to run the corpus is not stated, the thread link covers it; the meta description and the index card are unchanged. Status vocabulary: "in progress" (amber), "supported" (green), "not supported" (red). A test pins that every entry has project, status, date, url, source and both reading levels, that the section renders after Run history with the note once, and that the timeline event links the same permalink. Edited only; not committed or deployed.

**S-026 — Correction to S-021, S-023 and S-024: the JavaScript tests have been running in CI all along; they now run as their own `node --test` step, the S-023 textual pin is replaced by a behavioural test of the chart script, and the wrapper skips in CI so nothing runs twice.** Evidence: the log of run 35805463370 (job 107005165230, the S-025 commit) shows `test_function_builder … ok` and `test_vectors … ok`, not skipped: `setup-node` precedes the unittest step, so the Python wrappers found `node`. The three notes saying the JavaScript tests never run in CI, and the S-023 pin's comment "no node in CI", were wrong. What was true: the wrappers skipped silently when `node` was absent, and no line of the workflow named the tests. Now: `ci.yml` runs `node --test` between the Python suite and the build; Node's built-in runner discovers `tests/*.test.js` and `*.test.mjs` by name with no configuration or dependency, reports each file, and fails the step on a non-zero exit (checked locally with a deliberately failing script) or when `node` is missing. The Python suite keeps one wrapper, `JavaScript.test_node_test_runner`, running `node --test` as a local convenience; it skips without `node` and under `CI`, so a workflow run executes each JavaScript test once (owner's instruction; no `REQUIRE_NODE` variable). The scheduled jobs' validation step (S-024) is unchanged and does not get a `node --test` step: it runs the Python suite under `CI`, so the JavaScript tests skip there, and none of the three could catch a bad day's data. Checked by reading their inputs: `charts.test.js` builds its three-day history inline and loads `charts.js` under the module guard, which returns before the page's fetch of `history.json`; `alpha5-vectors.test.js` reads only the vendored `data/vectors/alpha5.json`, which no job writes; `activity-function.test.mjs` uses a fake fetch and no files. The checks that do read the day's data, the Python build tests over `data/tracker` and `data/library` and the failed-day rendering test, still run in that step. `charts.js` gains one line inside its existing IIFE (the file opens with `(function () {` and closes with `})();`, and already returns from that scope when the page has no chart mount), before the page bootstrap: under CommonJS it exports `SPECS`, `boolChart` and `lineChart` and returns; in the browser `module` is undefined and nothing changes. `tests/charts.test.js` renders the real `boolChart` and `lineChart` against an eight-line stub `document` and a three-day history (yes, no, and the 2026-09-22 shape: HTTP 500, `ok` false, `is_404` false) and asserts the rect classes `bool-true`, `bool-false`, `bool-null` in order, the tooltip and table cell "no data", that all seven specs carry an `okPath`, and that the line chart draws two points, not three. The S-023 textual pin (`test_bool_charts_treat_a_failed_day_as_no_data`) is removed: it asserted two substrings of the source, so any reformatting broke it while proving nothing about behaviour, and its stated reason was false; the new test covers strictly more, including the class names the stylesheet keys on. Noted, not tested here: `tests/activity-function.test.mjs` imports an ES-module `.js` (the Cloudflare Functions convention) without a package.json, which relies on Node's module-syntax detection, unflagged in Node 22.7 per Node's release notes; `setup-node` resolves "22" to the latest 22.x. Edited only; not committed.

**S-027 — Correction to S-026: the Node test's import of `functions/api/activity.js` does not rely on module-syntax detection; `functions/package.json` has declared `"type": "module"` since the function was created (commit f916cc6, 2026-09-21), and a test now guards that declaration.** S-026 said, labelled "not tested here", that the `.mjs` test imports an ES-module `.js` "without a package.json" and so depends on Node's unflagged syntax detection. Tested on Node 26.5 with copies of the two files: `module.findPackageJSON` names `functions/package.json` as the scope of `activity.js`, and a loader hook reports its format as `module`. With the declaration present the test passes with detection on and with `--no-experimental-detect-module`; with the declaration removed it passes only through the detection fallback, and with the fallback switched off it fails (exit 1, "Named export 'FRESH_SECONDS' not found … is a CommonJS module"). So resolution is by declaration, which every Node version `setup-node` can resolve for "22" honours (a `"type"` scope has been honoured since ES modules were unflagged in Node 12); older and newer Node were not run here (only Node 26.5 is installed and the Docker daemon is down; pulling images was not authorised). Rejected: renaming the function to `.mjs`, because the declaration already does what the rename would and Cloudflare's Pages Functions routing documentation shows only `.js` routes, so `.mjs` routing is undocumented and could not be tested (wrangler is not installed); pinning `node-version` (the run resolved "22" to v22.23.2 from the runner's tool cache), because the import no longer depends on the Node version and the pin would go stale, since Dependabot bumps action versions, not that input; a root `package.json` with `"type": "module"`, because it would flip `tests/alpha5-vectors.test.js`, `tests/charts.test.js` and the `require` of `alpha5.js` and `charts.js` (classic scripts with UMD guards) to ES modules and break them unless renamed `.cjs` or given their own `"commonjs"` scope. Done instead: `Activity.test_function_is_a_declared_es_module` fails if `functions/package.json` does not parse with `"type": "module"`, so losing the declaration breaks CI loudly rather than falling back silently; the Node test's import line carries a comment naming the declaration it relies on. The declaration is also what production has deployed since S-017, so its safety on Cloudflare is observed, not inferred. Edited only; not committed.

**S-028 (2026-09-23) — The `--rebuild-latest` test no longer pins live catalog values; it builds its own history and asserts shape. Also: `pr_172` added to `build.py`'s corpus dictionary.** `tests/test_tracker.py` copied the committed `history.json` and asserted `highest == 100789`, `is_404` and `nine_digit == 27`. Those are measurements that change with every catalogued object, and the test runs in the S-024 validation step of both scheduled jobs, so the first day the last-30-days group gained an object the tracker's own gate would have gone red, nothing would have been committed, and every later day would have failed the same way (found by the 2026-09-23 audit before the 06:17 UTC run; reproduced by appending a day with a new highest number). Now the test writes a three-day fixture history (a seed day, a scheduled day with different values, a failed day) and a two-source drift state into a temporary directory and asserts the output's shape: every top-level key, `latest` equal to the newest entry even when failed, every metric group present in `last_successful` with its fields plus `checked_at`, `entry_date` and `seed`, the latest successful day chosen per group (a group that failed on day 2 keeps day 1 and its seed flag), and the drift summary's counts over the fixture sources; a second test covers an empty history. The only numbers asserted are the fixture's own. Separately, three pages (home, findings, about) rendered `<a href="">` for PR #172 because the templates used `corpus.pr_172` and the dictionary had no such key while the library and activity pages carried the literal URL; the key is added. Edited only; not committed.

**S-029 (2026-09-23) — Batch C, site: wording that had gone stale, a count that needed its fetch time, and the dating of entries.** From the audit of 2026-09-23 (items 1.3, 1.5, 1.6 and 12). The about page's audit sentence says "128 recorded hashes (v0.1.0)", since the v0.2.0 manifest lists 139 sources and the audit covered v0.1.0. The findings page quoted 256 six-digit records "at the snapshot" while the home tiles show 231 for the same date: two fetches of the rolling last-30-days group, the corpus's seed fetch at 00:10 UTC and the tracker's run at 05:51 UTC; the findings sentence now carries its fetch time and says the tracker's later fetches show other counts with their own times, and the tile already showed its time. Six mentions of PR #172 as "awaiting review" (home in both reading levels, findings twice, library, about) now say "amended after review, awaiting merge" or its grammatical variant, since the maintainer reviewed it on 2026-09-22 and the amended head 9e8fc81 has been up since 2026-09-23; the audit counted six and a first pass of this change counted five, the home page's second reading level being the one missed. The site README's deploy section, written before the deploy, is marked done as of 2026-09-21 and kept as the record of the steps, with the custom domain marked attached. Dating: the preamble now says every entry from S-028 on carries its date after its number and that earlier entries are undated, with git history as their record; S-028 was the first dated entry, so the note names it rather than this one. No job, template logic or data changed; the test suite and the build pass. Edited only; not committed.

**S-030 (2026-09-23) — Batch B: the jobs and templates against bad days (audit items 11.2 to 11.9), one test per item.** Baseline: the first run through the S-024 gates, 2026-09-23 06:45 UTC, was clean (9 requests, 0 errors, four drift matches). Changes, each with a test in `tests/test_hardening.py`: (11.2) a 200 whose body is not the endpoint's data (an HTML page, an empty body, the provider's no-data text under 200, a CSV without a `NORAD_CAT_ID` column, a TLE body with no line 1) is recorded as `ok: false` with `error: "unexpected body: …"` and no count, so `last_successful` keeps the previous good day; before, it was `ok: true` with zeros, a green tile reading "TLE data returned", and the zeros were adopted. (11.3) a 404 whose body is not the provider's no-data text is an error, "404 with unexpected body"; `body_is_no_gp_data`, stored since S-009 and never read, now decides `ok`; `is_404` still records the status. (11.5) `main()` makes no request when a scheduled entry for the day already exists (a `workflow_dispatch` or "Re-run jobs" on the schedule day used to repeat all nine requests and replace the entry; `--force` repeats on purpose) and none on the day after a run in which every fetch failed and at least one answer was 403 or 429: it writes a `skipped` entry with the reason, the page shows it in a callout, and requests resume the following day; a run refused twice in a row therefore costs one request set per two days, not per day. (11.4) the Jinja environment uses `ChainableUndefined`, so a missing metric group renders as a dash instead of raising; the tracker page gates its tiles on a successful entry and says "No successful measurement has been recorded yet" otherwise, and the TLE tile on both pages says "no successful check yet" for a group that has never been ok instead of defaulting to "ok, some still fit" (the empty-history test caught that false reading during this batch). (11.6) the analyst tile prints the TLE count's own timestamp when it comes from an earlier day than the CSV count. (11.7) `library_status.behaviour()` records `ImportError`, `AttributeError` and `NameError` as `error` (the harness or the environment) and everything else the library raises as `fail` (the finding; #171's `TypeError` stays a failure); counts carry `error`; the library page's callout turns red and says the check could not run when a group errored or no behaviour ran, instead of a green "0 pass, 0 fail", and the run-history row says "could not run"; version cells show a dash rather than `None`. (11.8) the published `latest.json` had carried the four retracted "drift" verdicts until today's run rebuilt it; a test now pins `latest.latest` to `history[-1]` and `history_entries` to the length, so the file cannot lag again unnoticed. (11.9) both artifact-upload steps carry `if: ${{ !cancelled() }}`, so a run that crashed after making requests still preserves its data; a textual test pins it, the only way to test a workflow file here. Covered by a test: every item. Not covered by a test: the workflow step itself running under GitHub's semantics (pinned textually), and the 429 branch of the skip rule (exercised with 403; the code path is the same). Checked for this entry: the fifteen new tests failing before the code change (fourteen; the `latest.json` pin already held) and passing after, the full suite (44) with the exit code checked, the build, and the live data rendering the same tiles as before.

**S-031 (2026-09-23) — Site at corpus v0.2.1, DOI fields deliberately left at v0.2.0 until Zenodo mints.** `build.py` `version` is 0.2.1; `version_doi`, `version_doi_url` and `version_doi_version` stay at v0.2.0's, so the index and about pages render their transitional sentence ("the DOI for v0.2.1 is added once Zenodo has archived the release"), which is what the template was written for. `data/timeline.json` gains "Corpus v0.2.1 published" in the shape of the v0.2.0 entry (date 2026-09-23, time 2026-09-23T21:09:22Z, the GitHub release URL, commit b1407a5), described at each level as fixes to cases where the corpus reported a pass it had not checked, seventeen cases unchanged, one writer check added. `site/content/failures.json` is re-pointed at v0.2.1's `docs/FAILURES.md` (generated 2026-09-23T20:55:40Z) after a row-by-row check that every status and failing-item count equals the v0.2.0 table's, which is why the findings page's numbers (14 and 7 of 17, 20 failing items) stand. Stale-count grep over templates, content, data, jobs, README and handoff: no template carries a literal version or case count that changed; the handoff's state rows, its refreshed-repository item (which still described the reversed 563-of-565 correction) and its memory seed are brought to v0.2.1 and D-121. Tests, `node --test` and the build run with exit codes checked before the commit.

**S-032 (2026-09-23) — The v0.2.1 version DOI on the site.** `build.py`: `version_doi` 10.5281/zenodo.22926017, `version_doi_url` https://zenodo.org/records/22926017, `version_doi_version` 0.2.1, so the three fields are in step again and the index and about pages drop the transitional sentence S-031 relied on. `data/timeline.json` gains "Zenodo DOI minted" for v0.2.1 in the shape of the v0.2.0 entry (date only, no time, as that entry has; Zenodo's record creation time, 2026-09-23T21:48:05Z, is public and could be added if the convention changes), linking the DOI. The record appeared 39 minutes after the release, during a Zenodo outage period (corpus D-123 has the delivery evidence). The handoff's corpus row, open item and memory seed are updated. Tests, `node --test` and the build run with exit codes checked; the commit and push are the maintainer's.

**S-033 (2026-09-24) — Batch E3: the site for a first-time visitor (audit item 9).** Two "what do I do next" boxes, text approved by the owner with four changes (the Simple python-sgp4 box points at the corpus quick start instead of a bare command; the classification point says the field is overwritten, not a marker "turned back"; the Simple operator box drops "through no fault of your station"; the Technical operator box says what a correct reading of `A0000` is, 100000, not 0 and not an error). Box 1 sits at the foot of the library table, box 2 under the SatNOGS entry, each at both levels and ending in one action; every claim in them is a row of the table above or the entry's own attributed wording. One sentence of the approved operator text was corrected while building: it said objects above 99999 appear in CelesTrak's TLE output as Alpha-5 fields, but CelesTrak's TLE output omits them (the tracker's 404); the box now says CelesTrak serves them only in the OMM formats and Space-Track's TLEs carry them as Alpha-5 up to 339999. Also: the glossary gets an id and a route from the Technical level (a line at the end of the Technical migration page and one in the footer, both switching to Simple); the Simple hero gains a "Test your software" button to the corpus quick start; the findings table has a badge legend at Simple; the Technical status legend lists the 432 µs TLE-epoch tolerance beside the 2 µs and 1e-12 ones; PR #170 is "adds tests, does not change the library" at both levels on the findings and activity pages (the activity card gains a Technical paragraph); the drift tile headlines "N drift" or "0 drift" with errors in the line beneath, instead of "N error"; the audit's timeline entry and the about page state its v0.1.0 scope and that later additions were not separately audited; about's Simple provenance sentence now names the three sources the Technical level names (CelesTrak answers, the standard, labelled re-encodings); the two schema versions have one name each, "the SANA 2.0.0 schema set" and "the current 4.0.0 set". Tests, `node --test` and the build run with exit codes checked; the commit and push are the maintainer's.

**S-034 (2026-09-24) — The footer's AI-assistance sentence and its link are removed (owner instruction); the "How it was built" page is flagged, not changed.** `base.html`'s footer keeps "Maintainer: Honorius Neogy, NEOGY LLC. Code and text MIT." and drops "Built with AI assistance under human direction; see How it was built". Left as they are, for the owner's separate decision: the about page (`/about/`, nav label "How it was built"), whose description, Simple lede and Technical lede name AI assistance, whose Simple provenance paragraph says the kit and the site were built with an AI coding assistant under written rules and step-by-step human review and that the auditor was also an AI, and whose Technical section "AI assistance under human direction" describes the decision policy; the index card for that page, whose Technical text lists "AI assistance under human direction"; and the site README's line "Built with AI assistance under human direction". Without the footer line, the disclosure is no longer on every page, only on the about page, its index card and the README; the audit's scope statement on the about page ("The auditor was a separate AI session … not a human review") stays whichever way the owner decides, for the same reason the corpus keeps it (corpus D-126). Tests, `node --test` and the build run with exit codes checked; the commit and push are the maintainer's.

**S-035 (2026-09-24) — S-034 reverted: the footer's AI-assistance line is restored, exactly as it read before ("Built with AI assistance under human direction; see How it was built." with its link), by owner decision.** Reason: with the footer line gone, the disclosure was left only on the about page, its index card and the site README, and the owner chose consistency over partial removal: the disclosure stays on every site surface that already carried it, so the footer, the about page, the index card and the README agree again. The corpus-side narrative changes stand (corpus D-126: the README's "How this corpus was built" framing, the 0.2.1 changelog sentence and the release-notes draft), as does CITATION.cff as it is now. The line was taken from the pre-S-034 file itself, not retyped; `base.html` is byte-identical to its state at 5997ca0. Tests, `node --test` and the build run with exit codes checked; the commit and push are the maintainer's.

**S-036 (2026-09-24) — PR #172 merged upstream: the six sentences that said "awaiting merge", the library-table row for the empty `OBJECT_ID` and the timeline are updated; the tested count stays 7 of 17.** Verified before editing, from the GitHub API on 2026-09-24: PR #172 merged at 12:58:27Z by the maintainer, merge commit 8126f77 (parent bf25b00, head 0f20787), master identical to it; issue #171 closed at 12:58:28Z as completed; PyPI's latest sgp4 is 2.27 of 2026-07-03, so no release carries the fix. Changed: the PR bullet on the home page (both levels), the two findings paragraphs (Simple and Technical), the library page's Technical next-step box ("until a release carries PR #172") and its empty-OBJECT_ID bullet, and the about page's upstream list; every one now says merged 2026-09-24 (as 8126f77 in the Technical texts) and that the fix reaches users with the next release. The library table's row keeps its `fail` result, because the weekly job tests the installed release and 2.27 still fails; its upstream labels now read "issue #171 (closed 2026-09-24)" and "PR #172 (merged 2026-09-24; fixed in master, awaiting a release)", changed in `jobs/library_status.py` (the source the weekly job writes from) and identically in `data/library/latest.json`, so the next job run reproduces them; the row flips to pass by itself when a release with the fix is installed. Timeline: one event, 2026-09-24T12:58:27Z, "Pull request #172 merged", hand-formatted like its neighbours. Left as they were: the home page's findings card ("with a fix we submitted", still true), the library page's Simple next-step box ("until the fixes land", true of a release), the findings page's "python-sgp4 fails 7" line and the failures table (results against 2.27), and the activity page, whose badges come from the live `/api/activity` state. Tests, `node --test` and the build run with exit codes checked; the commit and push are the maintainer's.

**S-037 (2026-09-24) — Gpredict joins the ecosystem list as "not supported", on the maintainer's public statement of 2026-07-19 on PR #412; no Alpha-5 issue is filed (corpus Batch G, library 3).** Reworked before commit on the owner's instruction so that a reader finishes knowing the practical position, not just the failure: the entry states three facts in order (an Alpha-5 line that reaches Gpredict 2.6 decodes to 0; CelesTrak's TLE output omits objects above 99999, so the default feeds never carry one and a stock installation is missing those objects rather than misnumbering them; the maintainer is adding CelesTrak CSV support, the path by which they would arrive), cites the 2026-07-19 comment, and says the PR was withdrawn by its author, not declined. The entry cites what is public and verifiable by reading: the two `atoi`/`g_ascii_strtod` conversions of the catalog field in the 2.6 source, the withdrawn PR #412 (closed by its author the day it was opened), the maintainer's reply that CelesTrak would not, as far as he could tell, serve Alpha-5 and that CelesTrak CSV support is in progress, and issue #250. The status is "not supported" because 2.6 reads an Alpha-5 field as 0 and no change is planned for the TLE path; the maintainer's direction is the CSV format, which the entry says. The Simple text tells a Gpredict user the practical consequence, which the corpus's own case documents: CelesTrak's TLE output omits objects above 99999, so those satellites are absent from a Gpredict fetch rather than misnumbered. The date is the statement's date, the link the comment itself. Not written into the entry: the corpus's function-level run of Gpredict's parser module (all 604 Alpha-5 sets read as 0; every mean motion one digit short), because this list is labelled "statements, not results" and its test pins that label; the results live in the corpus's Batch G notes, and if the owner wants them on the page the label and its test change in their own entry. Tests, `node --test` and the build run with exit codes checked; the commit and push are the maintainer's.

**S-038 (2026-09-24) — The status table follows corpus D-129: `satcat-70000-cutoff` shows not-exercised for all three parsers; seventeen cases, sixteen exercised by a parser, one a data check.** `site/content/failures.json` (compact rows, edited textually): the case's row reads not-exercised three times; the provenance names the corpus commit the table now reflects (main at 71cf3d5, after v0.2.1, catalogue generated 2026-09-24T20:24:53Z) and states the sixteen-plus-one split, and the link points at that commit's `docs/FAILURES.md` rather than the v0.2.1 tag's, because the tag's table still shows pass for the row. The findings page's summary line says the reference passes all sixteen cases a parser exercises exactly and names the seventeenth as a data check; its Simple legend gains the not-exercised badge with a one-line meaning (the data-check case, and the nine-digit case outside a launch window); the CSS gives that badge the neutral style. The failing counts people cite, naive 14 of 17 and python-sgp4 7 of 17 with 76 and 20 items, do not move and are not touched. Tests, `node --test` and the build run with exit codes checked; the commit and push are the maintainer's.

**S-039 (2026-09-25) — Batch G Phase 2 results as a section of the library page, "Run once against the corpus", between the weekly job's run history and the ecosystem list; not a new page.** Reason: the page already answers one question, what the corpus has found in the software people use, in tiers, and the hand runs are a third tier between the job's rows (tested every week, versions refreshed) and the ecosystem entries (statements, dated, never run); a separate page would split "libraries" across two navigation items and part the Gpredict row from the ecosystem entry S-037 placed here. Readers tell the tiers apart by three signals in both levels: plain section titles ("Checked every week", new h2, "Run once against the corpus", "Elsewhere in the ecosystem"), each opening with a sentence on what it is and is not; different columns and badges (per behaviour with moving pass/fail and a "last check" time for the job, per library with the version run, the run date, a neutral "n of 17" badge and a filed-report link for the hand runs); and separate data with its own provenance, the job writing `data/library/latest.json` and the hand table reading `site/content/library-runs.json`, whose `_provenance` names the corpus commit, the runner and the decision entries, printed under the heading at the technical level. Columns: library (linked), version run (technical: exact commit or tag and what was compiled), date, cases failed (Simple: "n of 17"; Technical: the full pass/tolerance/fail/skip/not-exercised breakdown, since a library with no OMM reader skips nine cases and the count alone would mislead), the most serious finding in one line, and the report links. Both levels state that a count is a result for that version on that date, not a verdict on the project, that it does not measure how many users are affected, and that every finding was reproduced on the library's own code before it was reported. Test: every row carries a version, a date and a report link, and the page renders each row and the three statements. Alternative: a new page (rejected for the reasons above; the cost of the section is a longer page, kept readable by the headings).

**S-040 (2026-09-25) — Two owner instructions on S-039: PyEphem is left out of the table entirely, with no placeholder, until its report is filed (the D-134 convention: nothing about an unreported finding on a public page), the owner adding the row with its link before deploying; and one Simple-level sentence says the counts do not rank the libraries, since one that reads fewer formats has fewer cases it can fail, with the skips in the technical breakdown.** The test asserts PyEphem's absence so the row cannot arrive without a deliberate change to the test, and the provenance says rows are added as reports are filed, never before. Seven rows ship: satellite.js 7.1.0, Gpredict 2.6, gods-eye-view main ce671ce, SatDump 1.2.2 and master, libsgp4 master and PR #42, tle.js 5.0.3, astroz main d558933, all run 2026-09-24, each linking the report filed from the owner's account on 2026-09-24 or 2026-09-25 (satellite-js#185, gpredict#426 and the ecosystem entry, gods-eye-view#751, SatDump#1221, dnwrnr/sgp4 PR #42 comment and #45, tle.js#62, astroz#97 and #98). Built and tested locally; not committed or deployed until the owner's word.

**S-041 (2026-09-25) — PyEphem joins the "Run once against the corpus" table, first row, after its report was filed: `readtle()` returns `catalog_number` 0 for Alpha-5 element sets, https://github.com/brandon-rhodes/pyephem/issues/296, filed 2026-09-25T02:17:30Z from the owner's account on the owner's word.** Row: ephem 4.2.1, run 2026-09-24, 6 of 17 failed; the technical breakdown notes that the runner of that day reported the SATCAT data check as a pass, reported not exercised for every parser since D-129. The test that asserted PyEphem's absence (S-040) now asserts its presence and, as before, that every row links a filed report; the section's opening says eight libraries and the provenance names D-128 with D-132 to D-141. Tests and the build run with exit codes checked; commit and deploy on the owner's word.

**S-042 (2026-09-25) — The hand-run table gains the corpus's gate over this month's launches (corpus D-142) as a column, with a short form at the Simple level and the full headline at the Technical level, and the preamble states the practical position plainly: six of the eight read TLE only, CelesTrak leaves every object above 99999 out of its TLE feed, so their users are not affected today; the new satellites are absent, not misnumbered.** Values are read from the record counts each Batch G run recorded, not re-run, and the provenance says so. Short forms: "nothing to load from this feed" (PyEphem, satellite.js, Gpredict, gods-eye-view, tle.js, and SatDump 1.2.2), "only via CSV" (SatDump master, libsgp4 master), "reads them" (libsgp4 PR #42, astroz with "letter A only"); the full text carries per-format counts, the HTTP 404 fact, the snapshot date, count and id range, and the letter caveat, astroz's naming its own. The owner asked for four in the "nothing to load" group; the runner's rule makes it six, because gods-eye-view and SatDump 1.2.2 also read TLE only from CelesTrak's TLE feed, and the entry records that the count follows the rule. Wording names the behaviour and never grades, per D-142; the test checks every row has both forms, the snapshot facts, no grading word, and that the page states the practical position. Tests and the build run with exit codes checked; commit and deploy on the owner's word.

**S-043 (2026-09-25) — Footnote on the hand-run table: refused and dropped are not distinguished for the eight Batch G runs, which were recorded before the corpus's refusal channel (D-144); a "dropped" count in the gate column may include records the library refused with an error.** On the owner's instruction the eight runs are not re-run and the column keeps its counts; the footnote sits in the section's technical text and in the data file's provenance, and the test pins it. Runs made with the channel report the two apart. Tests and the build run with exit codes checked; commit and deploy on the owner's word.
